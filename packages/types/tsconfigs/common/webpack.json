{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "compilerOptions": {
    // Webpack is sync, so we'll act like CJS instead of ESM
    "module": "NodeNext",
    "moduleResolution": "NodeNext",

    // React should only realistically be used in Webpack modules
    "jsx": "react"
  }
}
